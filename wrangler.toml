name = "diacriticefix"
main = "src/index.js"
compatibility_date = "2024-01-01"
compatibility_flags = ["nodejs_compat"]

# Security headers are now set in the Worker code (src/index.js)
# No need for [headers] section in wrangler.toml